<div class="row">
    <div class="col-sm-4">
        <img class="card-image" {{bind-attr src=card.mtgImage}} />
    </div>

    <div class="col-sm-6">
        <h2>{{card.name}}</h2>

        <p class="card-text">
            {{breaklines card.text}}
        </p>
    </div>
</div>

